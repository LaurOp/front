import { HoverBDirective } from './hover-b.directive';

describe('HoverBDirective', () => {
  it('should create an instance', () => {
    const directive = new HoverBDirective();
    expect(directive).toBeTruthy();
  });
});
